

import React from 'react';

import styles from './index.less';

export default class Index extends React.Component {
    render () {
        return (
            <div className={styles.about}>Index Page</div>
        )
    }
}